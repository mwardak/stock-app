import { createTheme } from "@mui/material/styles";

const useStyles = createTheme({
  text: {
    marginTop: "20px",
    marginBottom: "20px",
    display: "block",
    textAlign: "center",
  },
});

export default useStyles;
